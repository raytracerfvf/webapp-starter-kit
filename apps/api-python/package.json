{
  "name": "@repo/api-python",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn api_service.main:app --reload --port 8000",
    "lint": "uv run ruff check .",
    "format": "uv run ruff format .",
    "typecheck": "uv run pyright",
    "test": "uv run pytest"
  }
}
